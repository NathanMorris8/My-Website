import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Nathan Morris | Tesla-Inspired Portfolio',
  description: 'A modern, minimalist portfolio website inspired by Tesla\'s design aesthetic',
  keywords: 'portfolio, web developer, designer, tesla-inspired, Nathan Morris',
  authors: [{ name: 'Nathan Morris' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}